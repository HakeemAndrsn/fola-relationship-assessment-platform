[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# Serve all routes from index.html for client-side routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
